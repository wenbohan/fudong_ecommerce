<h1>支付</h1>
<div class="row">
  <div class="container">
    <div class="page-header">
      <h4><i class="fa fa-credit-card"></i> 支付宝付款</h4>
    </div>
    <p>订单号: <%= @payment.orders.map { |order| "<strong>#{order.order_no}</strong>" }.join(', ').html_safe %></p>
    <ul class="list-group">
      <li class="list-group-item">
        <%= image_tag "alipay.png", width: 100 %>
      </li>
    </ul>

    <br />
    <div class="pull-right">
      <!--向支付宝支付链接发送一个Post请求, 不是Rails controller 处理的 action,-->
      <form action="<%= @payment_url %>" method="post">
        
        <strong>¥<%= @payment.total_money %></strong>
        <input type="submit" value="支付" class="btn btn-success btn-lg" />
      </form>
    </div>
  </div>
</div>
